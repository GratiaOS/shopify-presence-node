import { IdentityForm } from '../components/IdentityForm';

export function IdentityPage() {
  return (
    <section>
      <h1>Identity Layer</h1>
      <p>Define tone, rituals, templates, and policies for your store.</p>
      <IdentityForm />
    </section>
  );
}
